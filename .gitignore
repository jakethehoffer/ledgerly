node_modules/
dist/
coverage/
*.log
.DS_Store
.env
.env.local
.vitest-cache/
.claude/

# Runtime SQLite artifacts (LEDGERLY_DB_PATH defaults to ./ledger.db).
ledger.db
ledger.db-wal
ledger.db-shm

# Local launch-day notes (marketing copy — never committed).
LAUNCH.md
BUILD-STORY.md

# Local Claude project guidance (workflow notes, never committed).
CLAUDE.md
