name: Release

# Publishes a Docker image to GHCR on every `v*` tag push, plus a manual
# `workflow_dispatch` for republishing an existing tag (e.g. after fixing a
# Dockerfile regression). Multi-arch (linux/amd64 + linux/arm64) so M-series
# Mac operators and ARM cloud instances both get native binaries.

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Existing tag to (re)publish, e.g. v0.1.0'
        required: true

permissions:
  contents: read
  packages: write

# Don't let two releases race over the :latest tag.
concurrency:
  group: release-${{ github.workflow }}
  cancel-in-progress: false

# Opt JavaScript-based actions into Node.js 24 ahead of GitHub's June 2026
# forced upgrade. See ci.yml for the rationale.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  publish-image:
    name: publish docker image to ghcr.io
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Resolve target ref
        id: ref
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "ref=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
            echo "tag=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "ref=${{ github.ref }}" >> "$GITHUB_OUTPUT"
            echo "tag=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout at tag
        uses: actions/checkout@v6
        with:
          ref: ${{ steps.ref.outputs.ref }}

      - name: Derive image tags
        id: tags
        run: |
          TAG="${{ steps.ref.outputs.tag }}"
          VERSION="${TAG#v}"
          OWNER_LOWER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          IMAGE="ghcr.io/${OWNER_LOWER}/ledgerly"
          {
            echo "image=${IMAGE}"
            echo "tag_full=${IMAGE}:${TAG}"
            echo "tag_version=${IMAGE}:${VERSION}"
            echo "tag_latest=${IMAGE}:latest"
          } >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (multi-arch)
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ steps.tags.outputs.tag_full }}
            ${{ steps.tags.outputs.tag_version }}
            ${{ steps.tags.outputs.tag_latest }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.version=${{ steps.ref.outputs.tag }}
            org.opencontainers.image.licenses=Apache-2.0
            org.opencontainers.image.title=ledgerly
            org.opencontainers.image.description=Stripe-to-double-entry mapping engine for indie SaaS
